{{!-- <div class="row row-cols-1 row-cols-md-2 g-4">
  {{#each animals}}
  <div class="col">
    <div class="card">
      <img src="{{this.photo}}" class="img-thumbnail" alt="photo{{this.id}}">
      <div class="card-body">
        <h5 class="card-title">{{this.name}}</h5>
        <p class="card-text">{{this.description}}</p>
      </div>
    </div>
  </div>
  {{/each}}
</div> --}}

<div class="container">
  <div class="d-flex flex-row flex-wrap">
    {{#each animals}}
    <div class="card text-center mt-3" style="width: 18rem; margin-right: 10px;">
      <img class="card-img-top img-thumbnail" style="height: 200px;" src="{{this.photo}}" alt="photo{{this.id}}">
      <div class="card-body">
        <h5 class="card-title">{{this.name}}</h5>
        {{!-- <p class="card-text">{{this.description}}</p> --}}
        <a href="#" class="btn btn-dark">Подробнее</a>
      </div>
    </div>
    {{/each}}
  </div>
</div>
